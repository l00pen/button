const initialState = {
  label: 'Click me!',
  loading: false,
  success: false,
};

export default function buttonJSReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
